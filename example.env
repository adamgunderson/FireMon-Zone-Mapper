# FireMon Zone Mapper Environment Variables
# Copy this file to .env and update with your values

# FireMon server hostname or IP address
FIREMON_HOST=localhost

# FireMon username
FIREMON_USERNAME=admin

# FireMon password (keep this secure!)
FIREMON_PASSWORD=your_secure_password_here

# Domain ID (default is 1)
FIREMON_DOMAIN_ID=1

# Usage:
# Linux/Mac: export $(cat .env | xargs) && python firemon_zone_mapper.py --auto-create
# Or source the file: source .env && python firemon_zone_mapper.py --auto-create
# Rename only: source .env && python firemon_zone_mapper.py --rename-only

# Security Notes:
# - Never commit .env files to version control
# - Set appropriate permissions: chmod 600 .env
# - Add .env to your .gitignore file

# Zone Mapping Notes:
# - Cloud device interfaces (AWS, Azure) are automatically excluded
# - When interfaces have multiple zones, the last one is used
# - Example: ['VRF0', 'INET2_Label', 'Outside_2'] → uses 'Outside_2'
# - Rename-only mode adds zone names to segments without changing assignments